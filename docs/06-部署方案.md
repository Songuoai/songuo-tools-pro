# 松果工具箱 Pro - 部署方案

## 🚀 部署架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户访问                              │
│              tools.hefeiapp.top                          │
└────────────────────┬────────────────────────────────────┘
                     │ DNS (阿里云 DNS)
┌────────────────────▼────────────────────────────────────┐
│              Cloudflare CDN                              │
│         (SSL + DDoS 防护 + 全球加速)                       │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
┌───────▼────────┐      ┌────────▼────────┐
│   Vercel       │      │   Vercel        │
│   (前端)       │      │   (管理后台)     │
│                │      │                 │
│ - Next.js SSR  │      │ - Next.js SSR   │
│ - Edge CDN     │      │ - Auth 保护      │
└───────┬────────┘      └────────┬────────┘
        │ API                     │ API
        └──────────┬──────────────┘
                   │
         ┌─────────▼──────────┐
         │     Supabase       │
         │  (新加坡区域)        │
         │                    │
         │ - PostgreSQL       │
         │ - Auth             │
         │ - Storage          │
         │ - Realtime         │
         └────────────────────┘
```

---

## 📋 部署前准备

### 1. 域名准备

**域名：** tools.hefeiapp.top

**DNS 配置（阿里云）：**
```
类型    主机记录    记录值                      TTL
CNAME   tools       cname.vercel-dns.com       10 分钟
```

**Cloudflare 配置：**
- 添加站点到 Cloudflare
- 更新 Nameservers
- 开启 SSL（Full 模式）
- 开启 Always Use HTTPS
- 开启 Auto Minify（HTML, CSS, JS）

---

### 2. Vercel 账号

**注册：** https://vercel.com/signup

**计划选择：**
- 初期：Hobby（免费）
- 后期：Pro ($20/月)

**环境变量：**
```bash
# .env.production
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxx  # 仅服务端使用
NEXT_PUBLIC_SITE_URL=https://tools.hefeiapp.top
NEXT_PUBLIC_GA_ID=G-XXX
```

---

### 3. Supabase 项目

**创建项目：** https://supabase.com/dashboard

**配置：**
- 区域：ap-southeast-1（新加坡）
- 数据库：PostgreSQL 15
- 计划：Free（初期）→ Pro ($25/月)

**数据库初始化：**
```bash
# 运行迁移脚本
supabase db push

# 或手动执行 SQL
psql -h db.xxx.supabase.co -U postgres -d postgres -f docs/sql/001_init_schema.sql
```

**存储桶配置：**
```bash
# 创建存储桶
- tool-logos（工具 Logo）
- tool-screenshots（工具截图）
- user-avatars（用户头像）

# 设置公开读取
ALTER STORAGE BUCKET "tool-logos" SET PUBLIC = true;
ALTER STORAGE BUCKET "tool-screenshots" SET PUBLIC = true;
```

---

## 🏗️ 部署流程

### Phase 1：开发环境（本地）

```bash
# 1. 克隆项目
git clone <repo-url>
cd songguo-tools-pro

# 2. 安装依赖
npm install

# 3. 配置环境变量
cp .env.example .env.local
# 编辑 .env.local 填入本地配置

# 4. 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

---

### Phase 2：预览环境（Vercel Preview）

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 登录 Vercel
vercel login

# 3. 链接项目
vercel link

# 4. 部署预览
vercel --preview

# 获取预览 URL
# https://songguo-tools-pro-xxx.vercel.app
```

**GitHub 自动预览：**
- 每个 Pull Request 自动创建预览部署
- 预览 URL 评论在 PR 中

---

### Phase 3：生产环境（Vercel Production）

#### 方式 A：Git 推送部署（推荐）

```bash
# 1. 推送到 main 分支
git checkout main
git push origin main

# 2. Vercel 自动部署
# 访问 Vercel Dashboard 查看部署进度
# https://vercel.com/dashboard
```

#### 方式 B：CLI 部署

```bash
# 1. 生产部署
vercel --prod

# 2. 查看部署状态
vercel ls
```

#### 方式 C：GitHub Actions

```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install Vercel CLI
        run: npm install -g vercel
      
      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      
      - name: Build
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      
      - name: Deploy
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
```

---

## 📊 监控与告警

### Vercel Analytics

**启用：**
```bash
# Vercel Dashboard → Analytics → Enable
```

**监控指标：**
- Page Views
- Unique Visitors
- Bounce Rate
- Average Load Time

---

### Supabase 监控

**Dashboard：**
- Database CPU/Memory
- API Requests
- Storage Usage
- Auth Users

**告警设置：**
```bash
# Supabase Dashboard → Settings → Alerts
- CPU > 80% 持续 5 分钟
- Memory > 90% 持续 5 分钟
- Disk > 80%
```

---

### 自定义监控

**健康检查端点：**
```typescript
// app/api/health/route.ts
export async function GET() {
  const checks = {
    database: await checkDatabase(),
    supabase: await checkSupabase(),
    timestamp: new Date().toISOString()
  }
  
  const allHealthy = Object.values(checks).every(v => v === true)
  
  return Response.json({
    status: allHealthy ? 'healthy' : 'unhealthy',
    checks
  }, {
    status: allHealthy ? 200 : 503
  })
}
```

**Uptime 监控：**
- UptimeRobot（免费）
- 监控频率：5 分钟
- 告警通知：邮件 + 短信

---

## 🔄 CI/CD 流程

```
开发者
  │
  │ git push
  ▼
GitHub
  │
  │ Webhook
  ▼
Vercel
  │
  ├─→ 安装依赖
  ├─→ 运行测试
  ├─→ 构建
  ├─→ 部署
  │
  ▼
生产环境
  │
  │ 自动 SSL
  │ CDN 缓存
  ▼
用户访问
```

---

## 💾 备份策略

### 数据库备份

**Supabase 自动备份：**
- Pro 计划：每日自动备份
- 保留 7 天

**手动备份：**
```bash
# 导出数据库
pg_dump -h db.xxx.supabase.co -U postgres songguo-tools-pro > backup-$(date +%Y%m%d).sql

# 恢复数据库
psql -h db.xxx.supabase.co -U postgres songguo-tools-pro < backup-20260324.sql
```

**备份自动化：**
```bash
# crontab -e
0 2 * * * /path/to/backup-script.sh
```

---

### 存储备份

**Supabase Storage：**
- 定期下载重要文件
- 使用 Rclone 同步到本地

```bash
# Rclone 配置
rclone config
# 添加 Supabase S3 兼容存储

# 同步
rclone sync supabase:tool-logos ./backup/logos
```

---

## 🔒 安全配置

### SSL/TLS

**Cloudflare SSL：**
- Mode: Full (Strict)
- Always Use HTTPS: On
- Minimum TLS Version: 1.2
- Opportunistic Encryption: On
- TLS 1.3: Enabled

**HSTS：**
```
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
```

---

### 安全头

**Next.js 配置：**
```javascript
// next.config.js
async function headers() {
  return [
    {
      source: '/:path*',
      headers: [
        {
          key: 'X-DNS-Prefetch-Control',
          value: 'on'
        },
        {
          key: 'Strict-Transport-Security',
          value: 'max-age=63072000; includeSubDomains; preload'
        },
        {
          key: 'X-Frame-Options',
          value: 'SAMEORIGIN'
        },
        {
          key: 'X-Content-Type-Options',
          value: 'nosniff'
        },
        {
          key: 'X-XSS-Protection',
          value: '1; mode=block'
        },
        {
          key: 'Referrer-Policy',
          value: 'strict-origin-when-cross-origin'
        }
      ]
    }
  ]
}
```

---

### Rate Limiting

**Vercel 内置：**
- 免费计划：100 请求/秒
- Pro 计划：更高限制

**自定义限流：**
```typescript
// middleware.ts
import { Ratelimit } from "@upstash/ratelimit"
import { Redis } from "@upstash/redis"

const ratelimit = new Ratelimit({
  redis: Redis.fromEnv(),
  limiter: Ratelimit.slidingWindow(10, "10 s"),
  analytics: true
})

export async function middleware(request: Request) {
  const ip = request.headers.get("x-forwarded-for")
  const { success } = await ratelimit.limit(ip ?? "anonymous")
  
  if (!success) {
    return new Response("Too Many Requests", { status: 429 })
  }
}
```

---

## 📈 性能优化

### CDN 配置

**Vercel Edge Network：**
- 自动全球 CDN
- 40+ 边缘节点
- 自动压缩（Gzip/Brotli）

**Cloudflare 优化：**
- Auto Minify: On
- Brotli: On
- HTTP/2: On
- HTTP/3: On
- Early Hints: On

---

### 图片优化

**Next.js Image：**
```typescript
import Image from 'next/image'

<Image
  src={logoUrl}
  alt={toolName}
  width={200}
  height={200}
  priority
  quality={85}
  sizes="(max-width: 768px) 100vw, 200px"
/>
```

**格式转换：**
- 自动转换为 WebP/AVIF
- 响应式图片尺寸

---

### 缓存策略

**静态资源：**
```
Cache-Control: public, max-age=31536000, immutable
```

**动态内容：**
```
Cache-Control: private, no-cache, must-revalidate
```

**API 响应：**
```
Cache-Control: s-maxage=60, stale-while-revalidate=300
```

---

## 🎯 上线检查清单

### 部署前
- [ ] 所有测试通过
- [ ] 环境变量配置
- [ ] 数据库迁移完成
- [ ] 初始数据录入
- [ ] SSL 证书配置
- [ ] DNS 解析测试

### 部署后
- [ ] 网站可访问
- [ ] HTTPS 正常
- [ ] 所有页面加载正常
- [ ] 搜索功能正常
- [ ] 用户登录正常
- [ ] 统计代码生效
- [ ] 移动端适配正常
- [ ] SEO 元数据正确

### 监控
- [ ] Uptime 监控配置
- [ ] 错误追踪配置
- [ ] 性能监控配置
- [ ] 告警通知测试

### 运营准备
- [ ] 工具数据录入（100+）
- [ ] 专题内容准备（3-5 个）
- [ ] 社交媒体账号
- [ ] 客服邮箱配置
- [ ] 应急预案准备

---

## 💰 成本估算

| 服务 | 免费额度 | Pro 计划 | 预计月成本（初期） |
|------|---------|---------|------------------|
| Vercel | 100GB/月 | $20/月 | ¥0 |
| Supabase | 500MB DB | $25/月 | ¥0 |
| Cloudflare | 免费 | $20/月 | ¥0 |
| 域名 | - | ¥60/年 | ¥5 |
| **总计** | | | **¥5/月** |

**升级时机：**
- Vercel Pro：流量 > 100GB/月
- Supabase Pro：数据库 > 500MB 或 用户 > 50000

---

_文档版本：v1.0_
_创建时间：2026-03-24_
_创建者：SG:MKTMGR（松果市场经理）_
